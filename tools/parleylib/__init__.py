"""parley runtime: appender, harness adapters, dispatcher, CLI.

The schema (SCHEMA.md) and the protocol (PROTOCOL.md) are the product; this package is
the thin harness that drives them. tools/validate.py and tools/project.py are imported
as modules and remain usable on their own.
"""
